const calcInput = document.querySelector('.result');
const allButtons = document.querySelectorAll('.button');

allButtons.forEach(button => button {
    button.addEventListener('click', (e) => {
        
    }
})